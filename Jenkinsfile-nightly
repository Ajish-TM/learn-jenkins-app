pipeline {
    agent any

    stages {

        stage('Build Docker image') {
            steps {
                sh 'docker build -f /workspaces/learn-jenkins-app/ci/Dockerfile -t my-playwright .'
            }
        }
    }
}